import { SearchHealthRecordPipe } from './search-health-record.pipe';

describe('SearchHealthRecordPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchHealthRecordPipe();
    expect(pipe).toBeTruthy();
  });
});
